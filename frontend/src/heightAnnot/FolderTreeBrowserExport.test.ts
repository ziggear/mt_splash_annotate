import { describe, expect, it } from 'vitest'
import FolderTreeBrowser from './FolderTreeBrowser'

describe('FolderTreeBrowser export button', () => {
  it('exports the folder tree browser component', () => {
    expect(FolderTreeBrowser).toBeTruthy()
  })
})

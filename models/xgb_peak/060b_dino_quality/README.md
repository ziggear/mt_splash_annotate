# 060b XGBoost Model

Bundled files:

- `model.ubj`
- `feature_columns.json`
- `train_config.json`

The annotation backend fails `Prepare annotation` when these files are missing;
it does not use a legacy peak fallback.
